{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "aegis-draft/careerPlayerHeroStats.schema.json",
  "title": "Pro Career Player-Hero Stats",
  "description": "accountId -> heroId -> {games, winrate}. Pro tier-1 all-time (агрегат match details, без pub). База player×hero для Hero Synergy (назначение + UI). playerHeroStats — подмножество в rolling-окне.",
  "type": "object",
  "additionalProperties": {
    "type": "object",
    "additionalProperties": { "$ref": "#/definitions/stat" }
  },
  "definitions": {
    "stat": {
      "type": "object",
      "required": ["games", "winrate"],
      "additionalProperties": false,
      "properties": {
        "games": { "type": "integer", "minimum": 1 },
        "winrate": { "type": "number", "minimum": 0, "maximum": 1 }
      }
    }
  }
}
