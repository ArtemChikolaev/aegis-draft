// Package model — доменные типы сервера (users, saves, leaderboard entries, …).
// Пересекающиеся с игрой сущности держат ту же форму, что schema/ (единый accountId).
// Наполняется с T8.2+.
package model
