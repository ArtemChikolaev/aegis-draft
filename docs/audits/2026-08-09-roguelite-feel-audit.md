# Аудит 2026-08-09 — Roguelite Run: game feel против Balatro

## Паспорт

- Дата: 2026-08-09
- Наша версия / commit: `6209be7` + незакоммиченные параллельные правки (R14.9, `BALANCE_CONFIG_VERSION b1.26.0`) — аудит шёл на живом dev-сервере, который параллельно правила вторая сессия (HMR по ходу прогона, см. строку «wedged tournament»)
- Референс и URL/версия: Balatro — **принципы** juice/feedback (веб-разборы: [Balatro Design Analysis](https://medium.com/@yyh19971004/balatro-design-analysis-visual-packaging-and-interactive-feedback-cc6fa6a65370), [Juicy Feedback in a Poker Roguelike](https://blakecrosley.com/guides/design/balatro), [80.lv о карточной физике](https://80.lv/articles/balatro-s-card-movements-shaders-recreated-in-unity)) + собственный бриф [roguelite-balatro-brief.md](../roguelite-balatro-brief.md) (механика уже покрыта им; этот аудит — про **ощущение**)
- Scope: полный цикл Roguelite Run: config → cheat-модалка → драфт → этап 1 (field → groups → Show result) → Буткемп (Reward/Market/Build/Preparation) → этап 2 (field). Плюс код-хелс слоёв run-режима.
- Конфиг / seed / viewport: mode `run`, Team packs, Last 2 years, Event rating, Automatic, Cheat Mode ON; seed случайный (сид-поле принимает только run-код); desktop 1280×720, EN, dark
- Проверки и команды: живой прогон на `5273` (подключение к серверу параллельной сессии); анимации через `document.getAnimations()` (правило design-language, НЕ `getComputedStyle`); `wc -l` по `features/run` + `game/`; grep границ `game/ → ui|features|state` (пуст)

## Матрица

Ось сравнения — **обратная связь на событие игрового цикла**, а не контент. Механика (эскалация, экономика, боссы, конечный сезон) сверена ранее брифом и M5R и здесь не пересматривается.

| Сценарий / capability | Balatro: наблюдение (принцип) | Aegis Draft: наблюдение | Статус | Evidence | Решение / задача |
|---|---|---|---|---|---|
| Раздача карт рынка/наград | Карты влетают с каскадом, физика | `deal-in` 460ms, стаггер 45ms, сквозной индекс; вход раздела `fade-soft` | `parity` | `getAnimations()`: `_deal-in_` finished ×15, delay 0/45/90… | — (R14.4 ✅) |
| Idle-жизнь карточек | Фольга/блик редких изданий, лёгкий tilt при наведении | `card-foil` 7.2s и `ovr-shine` 2.6s на elite/rare тинтах; hover — только тень/подъём, tilt нет | `parity` (частично) | `getAnimations()`: running `card-foil`, `ovr-shine`; `camp.css:324` hover | R15.6 (P2) — hover tilt |
| **Покупка/апгрейд** (самое частое действие лагеря) | Карта физически улетает в билд, счётчик прыгает, звук слоями | Карточка **исчезает без exit-анимации** (grid схлопывает ячейку), Run power 92→93 **подменяется мгновенно** — без вспышки/пульса; вспышка есть только у золота (направление, R14.4) | `missing` | Живой Buy dyrachyo: после клика running-анимаций ноль (только ambient foil/shine); пентагон обновился без отклика | **R15.1 (P1)** |
| Момент «этап пройден» + payout | Cash-out: экран выплаты, счёт монет, штампы | Статичная строка в шапке лагеря «Stage 1 cleared · Paid out +3 prize · +3 for placement»; переход — `fade-soft` 360ms | `missing` | Переход To Bootcamp: единственная анимация `fade-soft` 360ms | **R15.2 (P1)** |
| Переход этап ↔ Буткемп | Смена сцен срежиссирована | Мгновенная подмена экрана (уже записано в R14 «Осталось», уровень `app/App.tsx`) | `missing` (уже tracked) | R14 ⬜ «Переход этап ↔ Буткемп» | R15.2 поглощает |
| Reveal турнира (группы/плей-офф) | Каждый счёт — событие: числа, звук, эскалация | Строки Match results **появляются без анимации**; из running — только `live-pulse` 1s; свой матч не выделен, финальная таблица без «печати» результата | `missing` | `getAnimations()` во время групп: `[live-pulse:1000]` и пусто после | **R15.3 (P1)** |
| Тряска/impact на больших событиях | Shake растёт с масштабом события; отключаем в настройках | Нет нигде | `missing` | grep `shake` по `web/src` пуст | R15.4 (P2) |
| Звук | Слои SFX, питч растёт с комбо; звук = половина ощущения | Нет вообще (ни одного Audio/WebAudio вызова) | `missing` | grep `Audio\|audio` по `web/src` — только нет SFX-слоя | R15.5 (P2, отдельный слой) |
| Эскалация фидбека с масштабом | Больше событие → больше слоёв (анимация+шейк+звук+частицы) | Все события визуально равнозначны | `missing` | Сравнение Buy(+0.3) и Buy(+1.8), clear stage vs обычный шаг — отклик одинаков (никакой) | Принцип в R15, применяется в R15.1–R15.5 |
| Победа сезона / смерть забега | Кульминация с секвенцией | `SeasonVictory.tsx` есть (113 строк + css); живьём не дошёл; Run over не проверен | `unknown` | Не воспроизведено в этом прогоне | Проверить в чистой сессии при реализации R15.2 |
| Боссы/Elite видимы заранее | Босс-блайнд виден и пугает | `StageKindBadge` + «↑ Field is stronger» на этапе 2 — информация есть, драматургии нет | `parity` (инфо) | Текст этапа 2 | Синг босса — внутри R15.3/R15.4 |
| Cheat-модалка, Show result, Skip phase | — | Работают: модалка с честным предупреждением, `Show result` довёл до финальной таблицы одним кликом | `parity` | Живой прогон | — (R1.1/R2 ✅) |
| Продвижение после полного reveal групп | — | ⚠️ Один раз после reveal групп **пропали все action-кнопки** (ни Skip/Show result/advance); после reload+resume кнопки вернулись и цикл продолжился | `unknown` | Прогон шёл под HMR параллельной сессии (правки `engine.ts` на лету) — грязная среда | Чистый repro нужен до заведения бага; если воспроизведётся без HMR — это P0 |

Код-хелс (вне матрицы): границы слоёв чистые (`game/` не импортирует `ui/features/state` — grep пуст); a11y камп-карточек в порядке (`CardInspectTrigger` — прозрачная кнопка с aria-label, `CampCards.tsx:113`; section nav — корректный `tablist`). Монолиты по `wc -l`: `runStore.ts` 1236, `CampScreen.tsx` 1205, `camp.css` 1170, `anteEconomy.ts` 1125, `TournamentScreen.tsx` 982 — декомпозиция CampScreen уже записана (R13.4/R14.2, Build/Preparation/Reserve намеренно не тронуты); новых записей не плодим.

## Приоритеты

- P0 — нет (кандидат «пропавшие кнопки после reveal групп» остаётся `unknown` до чистого repro).
- P1 — R15.1 (отклик покупки), R15.2 (секвенция «этап пройден» + переход), R15.3 (reveal турнира как драматургия).
- P2 — R15.4 (shake-бюджет), R15.5 (звуковой слой), R15.6 (hover tilt), проверка victory/run-over моментов.

## Синхронизация

- PRD: §5.9.4 — game feel как продуктовое требование Roguelite Run (соразмерный отклик на каждое значимое событие; звук — отдельный слой с тумблером).
- BACKLOG: новая секция **R15** в M5R с конкретикой реализации (файлы, тайминги, ограничения).
- Skill / rule: новый скилл `game-feel-juice` (мothion-правила design-language + запреты из шишек R14.4/R14.9 + эскалация + звук); INDEX.md обновлён.
- Исправлено сейчас: ничего (аудит без правок кода — параллельная сессия активно меняет `engine.ts`/`MarketPanel.tsx`).
- Отложено и почему: всё R15 — код-правки конфликтовали бы с параллельной сессией; «расширение развития» НЕ заводим новыми системами — путь уже записан (R13.5 Editions `Charged`, R12.6 потолок билда, T6.4 Stakes), новые сущности рядом с ними были бы дублированием.

## Повторная проверка

- [x] воспроизведение больше не падает — n/a, код не менялся;
- [x] чистая логика / unit tests — не запускались: правок кода нет, а рабочее дерево держит чужие незакоммиченные правки (их прогоняет параллельная сессия);
- [x] UI golden path — живой прогон цикла до этапа 2 пройден;
- [x] typecheck/build — n/a (нет правок кода);
- [x] документы не противоречат реализации — PRD §5.9.4, BACKLOG R15, INDEX/скилл синхронизированы этим аудитом.
